# global:
#   security:
#     allowInsecureImages: true   # required by newer Bitnami charts when not using official Bitnami images
  
serviceAccounts:
  create: true
  name: external-dns

sources:
  - ingress

domainFilters:
  - lab.ridwanahmed.com

provider: aws
aws:
  zoneType: public
  region: eu-west-2