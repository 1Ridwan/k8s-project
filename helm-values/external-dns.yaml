global:
  security:
    allowInsecureImages: true   # required by newer Bitnami charts when not using Bitnami images

image:
  registry: registry.k8s.io
  repository: external-dns/external-dns
  tag: v0.15.1

provider: aws
sources:
  - ingress

domainFilters:
  - lab.ridwanahmed.com

aws:
  zoneType: public
  region: eu-west-2