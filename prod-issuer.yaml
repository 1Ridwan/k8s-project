apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-production
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: ridwan157@outlook.com
#    profile: tlsserver
    privateKeySecretRef:
      name: cert-manager-secret
    solvers:
    - dns01:
        route53:
          region: eu-west-2
          hostedZoneID: Z0717143R661NCU61KOX
    - http01:
        ingress:
          class: nginx