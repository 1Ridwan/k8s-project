# simple app so use slim alpine image 
# alpine Linux is much smaller than most distribution base images (~5MB)
# pin version
FROM nginx:1.29.3-alpine-slim 


COPY ./ /usr/share/nginx/html

EXPOSE 80
